# Base URL of the Cloudflare Worker rates API. Trailing slash optional (the app normalizes it).
# Local dev: `wrangler dev` serves the worker on http://localhost:8787/.
# Production: the deployed worker URL, e.g. https://<name>.workers.dev/
VITE_API_URL=http://localhost:8787/
