# Base URL of the Cloudflare rates worker.
# Local dev (`wrangler dev`) serves the worker on http://localhost:8787.
# For a physical device, use your machine's LAN IP instead of localhost.
# For production builds, use the deployed https://<name>.workers.dev/ URL.
EXPO_PUBLIC_API_URL=http://localhost:8787/
