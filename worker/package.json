{
  "name": "tsukakan-rates",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Zero-cost, keyless Cloudflare Worker proxying public fiat and crypto rates.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "start": "wrangler dev",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.5",
    "@cloudflare/workers-types": "^5.20260716.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.0",
    "wrangler": "^4.111.0"
  }
}
